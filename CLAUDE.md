# ygo-sniper 專案守則

遊戲王 1998-2004 PSA/ARS 鑑定卡的自用撿漏器。掃四個賣場、算到手成本、估公允價、
給出價上限、推 Telegram。**使用者會照著這裡產出的數字花真錢**，所以下面每一條
都對應一次真實出過的錯。

動手前先讀完。這份檔案的每一行都是繳過學費換來的。

---

## 一、最貴的一課：誤殺是靜默的，雜訊是看得見的

過濾規則寫錯的兩個方向，代價完全不對稱：

- **誤殺（漏掉真標的）**：沒有任何痕跡。使用者不會知道有東西被丟掉，只會覺得
  「最近沒好貨」。而漏掉的可能正是最該看到的那一筆。
- **誤放（雜訊進來）**：看得見、可以再修、使用者自己一眼就分辨得出來。

**所以判斷不確定時，一律偏向不過濾。**

實際被這條規則救回來的（每一個都是「看起來很合理的規則」差點誤殺真卡）：

| 差點加入的規則 | 會誤殺什麼 |
|---|---|
| `looks_like_yugioh` 正面表列 | 5 筆真卡（`ブラックマジシャンガール 初期 ウルトラ P4-01`…） |
| 排除 `旧枠` | 遊戲王賣家自己在用（`ネオスペーシアン E・HERO 旧枠 旧テキスト`） |
| 排除 `サッカー` | 5 張**不同**真卡（`ブラッド・サッカー`、`シャーク・サッカー`…） |
| 排除 `スターカード` | 是 `モンスターカード` 的子字串，誤殺 17 筆 |
| 排除 `NBA` / `BASEBALL` | `CANNONBALL` / `ULTIMATE BASEBALL KID` |
| 商號詞加 `アルス` | 有賣家用它稱呼 ARS 鑑定（一筆 NT$19,366 的真實成交） |

**而且不只是「差點」——已經有一個活著誤殺很久才被抓到**（2026-08-05，
`corpus-diff` 上線第一跑就抓到）：排除字 `プリズマ` 是真卡名 `プリズマン`
的子字串，殺掉了 `PSA9 遊戯王 プリズマン 初期 ノーマル Vol.6`——PSA9、初期、
Vol.6，正中目標輪廓。它要擋的現代稀有度用 `プリズマティック` 早就涵蓋了，
`プリズマ` 純屬冗餘。**沒有工具就沒人會發現**，這正是誤殺靜默的證明。
現在有一條不變式釘住整類錯誤：`tests/test_corpus.py` 的
`test_no_era_card_name_is_hit_by_an_exclude_keyword` —— **年代內真卡名
被排除字命中的正確值只有一個：空集合。**

**改任何過濾規則的驗收協定（不可省略）**：

1. 對**全語料**跑雙向比對——用 `ygo-sniper corpus-diff`，三步順序不能換：

   ```bash
   ygo-sniper corpus-diff --save-baseline   # ← 改規則**之前**先跑
   # …改規則…
   ygo-sniper corpus-diff                   # ← 改完跑，列出每一筆改變
   ```

   語料 = `signals` + `comps` + `listing_obs` + `data/cache` 的標題（約 31,000 筆）
   **＋ `data/cards_1998_2004.json` 的 15,394 個卡名**。第三桶不可省略：市場上
   不是每天都有那張卡在賣，只掃在架標題會漏掉「排除字命中真實卡名」這一整類誤殺
   （實測 `サッカー`：標題語料 0 命中，卡名主檔 5 命中）。
2. 報「新擋掉幾筆」**與「其中誤殺幾筆」**——`corpus-diff` 把改變全部列出來，
   但**它不會替你宣稱誤殺 0**（哪些算真卡要人看）。唯一的例外是
   「排除字命中年代內卡名」：那不需要判斷，命中就是誤殺
3. **誤殺數必須是 0**。不是 0 就縮小規則，不要放行
4. 已知會誤殺的候選詞要寫成**紅燈測試**，防止日後有人憑印象加回去
   （見 `tests/test_exclude_other_tcg.py`）

寧可漏擋幾筆古書或球員卡，不要漏掉一張真卡。

---

## 二、`\b` 對 CJK 無效——而且拿掉它可能弄壞別的東西

漢字與假名在 Python `re` 裡算 `\w`，所以 `PSA鑑定品` 的 `A` 與 `鑑` 之間、
`1999年` 的 `9` 與 `年` 之間**都沒有 word boundary**。

`src/ygo_sniper/parsers/` 因為這件事出過**四次**事故：

1. `\bPSA\b` 抓不到「PSA鑑定品」→ 整批只寫機構不寫分數的貨被丟掉
2. `_hit_any` 用純子字串比對 → `"PS"` 命中 `PSA10`、`"AST"` 命中 `LAST`、
   `"LOB"` 命中 `GLOBAL`，年代證據可以被任何英文單字偽造
3. `\b(199[89])\b` 抓不到「1999年」→ 最硬的年代證據被判為「無證據」
4. `\bPSA\b` 意外擋住了 `PSA10相当`（賣家宣稱「相當於 PSA10」，實際是 ARS 鑑定）
   —— **修好第 1 項之後才暴露出來**

第 4 項是最重要的教訓：**那個 `\b` 不完全是 bug，它同時在做一件對的事。**
拿掉守衛之前，先查清楚它意外擋住了什麼。

**規則**：`src/` 底下的 regex 一律用 lookaround（`(?<![A-Za-z0-9])` /
`(?![A-Za-z0-9])` / `(?!\d)`），不用 `\b`。改動時跑全語料回歸，
四類既有行為必須保住（見 `tests/test_parsers.py` 的 `TestTitleGradeBoundary`
與 `TestGraderIsNotAShopName`）。

---

## 三、比較的兩個值必須同源、同基準（本專案最常犯的錯）

全域工程原則第一條在這個專案出現了**六次**，每次換一個地方：

| 事故 | 混了什麼 | 症狀 |
|---|---|---|
| Yahoo 價格語意 | 現在価格（會漲）當成可成交價 | 大量假 `FREE_CARD`，每筆都像撿到寶 |
| venue 混池 | Mercari 與 Yahoo 的價格水準差 2-2.6 倍 | 「市場整體漲了」的假象，其實是來源組成變化 |
| 賣家同儕比價 | 競標中出價 vs 別人的一口價 | 可比數 31→91，多出的六成全是假折價 |
| 套利路徑 | 日本境內價差 vs 貨在台灣 | 國際運費付兩次，價差蒸發 |
| 幣別 markup | eBay 台幣（要套）vs Mercari 台灣台幣（不套） | 判準不是幣別，是「會不會以外幣請款」 |
| 價格上限 | 日圓算出的上限套到 eBay 的美元／台幣 | 過濾條件整個錯位 |
| 成交型態混池 | 競標結標價（買家搶到多高）vs 定價成交（賣家開多少） | 排行榜第 5 名的 60.6 分**三個計分點全是混池** |

第七項（2026-08-06）值得單獨記：`basis="sold"` 看起來已經是「同一把尺」了，
但那一桶裡混著**兩種價格形成機制**——ヤフオク落札價是買家喊上去的（賣家只設了
開始価格），フリマ／Mercari／一口價即決才是賣家開的。實測 468 個計分點裡 251 筆
是競標結標（53.6%）。修法：`comps.sale_kind`（`auction`／`fixed`／`unknown`）
進同儕鍵，`unknown` 完全不進比較。**「同源」要問到機制那一層，不是問到欄位名為止。**

**症狀的共同點：混源比較的錯誤方向通常是「看起來很划算」**，所以它不會被使用者
的直覺攔下來，只會被對帳攔下來。

**規則**：任何相減、相除、比大小之前，先問「這兩個數字是同一把尺量出來的嗎」。
不同基準就分開呈現，不要合成一個數字。反解（例如出價上限）必須與正算走
**同一組參數、同一個 fx 物件**，並且**算完用正算驗回去**
（見 `bidding.py` 的 `FORWARD_CHECK_TOLERANCE_TWD`）。

---

## 四、不要拿自己的模型當尺去量別人

Seller Alpha 一開始的設計是「賣家售價 vs 我們模型的公允價」。實測發現模型有
**分段偏誤**（`L3 × Mercari` 那個切片高估 5.9 倍），於是**任何專賣便宜普卡的賣家
都會長得像持續低於市場價**——每個特徵都符合「系統性 alpha」的假說，只是那個
「市場價」是我們自己算錯的。

決定性的證據：`ebay:collectiblemore` 模型絕對法說 0.57×（看起來是 alpha），
同儕相對法說 **1.59×（比同儕貴 59%）——符號相反**
（出處 `seller_alpha.py:14-22`；倍數會隨資料變動，要引用時去讀當下的值，
不變的教訓是**符號會相反**）。更根本的原因：comps 表裡**一筆 eBay 成交都沒有**，
平台係數對 eBay 根本估不出來。

**規則**：評估「某人比市場便宜多少」時，用**同儕相對**（同卡×同版次×同稀有度×
同分數×同價格基準的其他賣家中位），不要用模型估值。兩邊都來自市場，模型偏誤
在相除時抵消。同儕湊不出來就**不計分**，不要退回用模型頂替。

例外（使用者明確要求）：**單筆通知**可以用模型 fallback，但要過既有的證據閘門、
標明判定來源、且**不得回饋到賣家分數**。理由是兩者的錯誤代價不同——通知錯了
使用者自己看一眼就知道，賣家分數錯了會長期扭曲注意力配置。

---

## 五、靜默失敗是這個專案的頭號敵人

排程工具最致命的坑：**爬蟲壞掉三週，使用者只會看到三週的「今天沒好貨」。**
兩者的外顯行為一模一樣，而只有前者需要修。

已經發生過的靜默失敗：

- `raise_for_status()` 對 `202 + 空 body` 不報錯（AWS WAF 挑戰）→ 回傳空字串當成功
- `category_id` 只傳給舊介面，而 Buyee 有 `search_detailed` → 分類設定形同虛設，
  「沒生效」與「生效了但今天貨就這些」外顯相同
- `recalc-bids` 重建 Listing 時手工列舉欄位漏了 `end_time` → 競標倒數全部失效
- `_upsert_listing_obs` 無條件覆寫 `seller_id` → 例行掃描把挖回來的賣家抹成 NULL
- eBay 競標 `price` 是 null → `return None`，整批競標從未進過系統
- `sold_at` 存的是入庫時間 → 90 天視窗對那批資料形同虛設

**規則**：
1. 失敗一律**大聲拋出**並分類（transient 可重試 / semantic 不重試 / blocked），
   絕不回傳空值假裝成功（見 `sources/base.py` 的 `FetchError` / `BlockedError`）
2. 「0 筆」必須能分辨四種情況：真的沒貨（`EMPTY_CONFIRMED`）／解析壞了
   （`PARSER_BROKEN`）／被擋（`BLOCKED`）／連線失敗（`FETCH_FAILED`）
3. **canary 是唯一不會跟著對方改版一起壞掉的判準**——用一個必定大量命中的
   關鍵字自檢，而且判定要看 `parsed_count`（解析器解出幾個）而不是
   `len(listings)`（商業篩選後剩幾個）。量錯東西的指標比沒有指標更糟：
   曾經因此產生 12 次假的 `parser_broken` 告警
4. 重建物件時**不要手工列舉欄位**，用 dataclass 反射——手寫清單一定會跟定義漂移

---

## 六、測試路徑必須等於生產路徑

兩次出事：

1. **fixture 用 Playwright 抓（JS 執行完），生產用 httpx 抓（JS 不跑）**
   → parser 在測試裡完美運作，線上只抓得到 `loading-spinner.gif`。
   現在 `tests/fixtures/` 的 Buyee 檔案有一條結構性防線：**必須含
   `loading-spinner`**，誰再用 Playwright 抓 fixture 就會被測試擋下來。
2. **`serve` 指令從來沒被端到端跑過**：`uvicorn.run("web.app:app")` 需要
   `web` 在 `sys.path`，但 console script 的 `sys.path[0]` 是 `.venv/bin`。
   我驗證時手動加了 `sys.path.insert(0,'.')`，那行正好掩蓋了真正的問題。

**規則**：驗證「使用者實際會打的那個指令」，不是「元件會不會動」。
fixture 一律用生產路徑抓取。

---

## 七、樣本數 ≠ 證據強度（估價模型專屬）

`Estimate.n_effective` 是**所用退化層級的池大小**，與誤差**反向**相關：

```
n=1   → 多半是 L1（找到同一張卡的成交）      ← 最強
n=325 → 是 L3（連卡名都沒認出，退到稀有度池） ← 最弱
```

實測 `n<3` 那一桶的下尾違反率只有 2%，是全部分桶裡**最保守**的一群。
把 `min_effective_samples` 調高會砍掉最好的估計、留下最差的。

判斷證據強度看 **level（L1/L2 vs L3/L0）＋ 分數是否已知 ＋ 校準群樣本數**，
不要看 `n_effective`。要調任何門檻之前，先跑 `ygo-sniper coverage-groups`
重新量一次，不要照直覺調。

---

## 八、出價上限的紅線（會花真錢的地方）

使用者的承諾是「設上限、絕不追價」，所以唯一要守的事是**上限本身不能算錯**。

- 上限一律從 **conformal 80% 區間的下緣**反推，不是點估計
  （點估計中位誤差 ×1.9，拿它當上限等於一半機率出價過高）
- **證據不足時不准輸出數字**——四道閘門（分數已知／L1-L2／校準樣本足夠／
  不在已知破口分桶）任一不過就回「證據不足」＋說明還缺什麼
- 反解必須與正算同源，算完**用正算驗回去**，對不上就拒絕輸出
- eBay 的上限用**原幣**輸出（使用者要填進出價框的數字），換匯用
  **這筆 listing 自己的比率**（`value/convertedFromValue`），不是我們的 fx 表
- **工具只計算，不碰錢**：沒有任何自動出價／自動下單功能

「大多數競標會輸」是設計不是缺陷——輸的成本是零，贏的每次都在安全邊際內。

---

## 九、常用指令

```bash
make test                      # pytest（目前 1339 passed，2026-08-06 實測）
ygo-sniper daily               # 掃描＋推播（launchd 排程跑這個）
ygo-sniper scan --dry-run      # 只掃不寫庫
ygo-sniper serve               # dashboard → http://127.0.0.1:8321
ygo-sniper notify-preview      # 只算不送，調門檻時用這個
ygo-sniper health              # 各來源健康 ＋ 告警表
ygo-sniper sellers --rank      # Seller Alpha 排行榜
ygo-sniper seller <key>        # 單一賣家 drill-down
ygo-sniper watch-seller list   # 監控名單
ygo-sniper coverage-groups     # 分群覆蓋率（調門檻前必跑）
ygo-sniper backfill-sale-kind  # 回填 comps 成交型態（競標結標／定價成交），冪等
ygo-sniper corpus-diff         # 全語料雙向比對（改過濾／解析規則前後必跑，見第一節）
ygo-sniper venue-study         # 平台價差研究
make schedule / make unschedule # launchd 排程（分時段，一天 15 個觸發點）
```

排程時段：白天 09:30-17:30 每 2 小時（抓新上架）、**18:00-22:30 每 30 分**
（結標高峰，實測所有競標都在台灣時間 18-22 點結標）、夜間不跑。

---

## 十、動手前的自我檢查

1. 我要改的是**過濾規則**嗎 → 跑全語料雙向驗證，誤殺數必須 0
2. 我要動 **regex** 嗎 → 用 lookaround 不用 `\b`；先查這個守衛意外擋住了什麼
3. 我要**比較兩個數字**嗎 → 它們同源嗎？同單位嗎？同一個物理位置嗎？
4. 我要**新增一個數字給使用者看**嗎 → 證據不足時它會不會誠實地拒絕輸出？
5. 我改完**驗證了嗎** → subagent 的回報不算證據，主對話要親跑指令看輸出
</content>
